System name:
tanodragon

Full system name:
Tano Dragon

Supported file extensions:
.cas .CAS .ccc .CCC .dsk .DSK .rom .ROM .7z .7Z .zip .ZIP

Launch command:
%STARTDIR%=~/.mame %EMULATOR_MAME% -rompath %GAMEDIR%\;%ROMPATH%/dragon32 tanodr64 -autoboot_delay "4" -autoboot_command "cloadm\"\"\n" -cass %ROM%

Alternative launch commands:
%STARTDIR%=~/.mame %EMULATOR_MAME% -rompath %GAMEDIR%\;%ROMPATH%/dragon32 tanodr64 -cart %ROM%
%EMULATOR_XROAR% -fs -default-machine tano %ROM%

Platform (for scraping):
dragon32

Theme folder:
tanodragon
